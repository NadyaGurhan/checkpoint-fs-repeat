const fs = require('fs');

function getNames() {
const arr = 
}
console.log(getNames);

module.exports = {
  getPadawanNames,
  getLightsaberScores,
  getStats,
  writeStats,
};

// Я поняла, что нужно найти ошибку в тесте и исправить ее. Проблема в том, что я даже не знаю как начать. Я запустила npm test, 
//увидела ошибку, а что дальше делать я не знаю. 

const fs = require('fs')
module.exports = {
  getPadawanNames,
  getLightsaberScores,
  getStats,
  writeStats,
};


// Чекпоинт пуст, еще за вчерашний день ничего не понял, буду отрабатывать на выходных
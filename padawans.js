const fs = require('fs')
module.exports = {
  getPadawanNames,
  getLightsaberScores,
  getStats,
  writeStats,
};

const padawans = console.log(fs.readFileSync('./data/padawans.txt', 'utf-8'))
function getPadawanNames () {
  padawans.join
}

getPadawanNames()

const evaluation = console.log(fs.readFileSync('./data/scores.txt', 'utf-8'))
function getLightsaberScores () {
  evaluation.join
}

getLightsaberScores()

function getStats () {
  table.push({getPadawanNames : getLightsaberScores})
}

getStats()



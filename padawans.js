const fs = require('fs');

module.exports = {
  getPadawanNames,
  getLightsaberScores,
  getStats,
  writeStats,
};

const str = fs.readFileSync('./');

// <!-- Если быть честным, не особо разобрался с заданием, подскажите какую литературу можно почитать.

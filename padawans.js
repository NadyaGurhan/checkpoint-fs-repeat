const fs = require('fs');

module.exports = {
  getPadawanNames,
  getLightsaberScores,
  getStats,
  writeStats,
};

Надо написать функции, которые будут проходить тесты. Функции я бы разнесла по отдельным файлам файлы создала). Не разобралась, как написать функцию, которая будет вытаскивать данные из файла data/padawans.txt

